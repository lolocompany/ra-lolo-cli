import { CreateProps } from 'react-admin'
import { ResourceToolbar } from '@components/ResourceToolbar';

const {{resourceName}}Create: React.FC<CreateProps>  = (props) => (
  <Create actions={<ResourceToolbar backButton />} {...props}>
    <SimpleForm>
    {{#each components.createView}}
      {{{this}}}
    {{/each}}
    </SimpleForm>
  </Create>
);

export default {{resourceName}}Create;