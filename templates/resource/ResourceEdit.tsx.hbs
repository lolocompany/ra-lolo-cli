import { EditProps } from 'react-admin'

const {{resourceName}}Edit: React.FC<EditProps>  = (props) => (
  <Edit {...props}>
    <SimpleForm>
    {{#each components.editView}}
      {{{this}}}
    {{/each}}
    </SimpleForm>
  </Edit>
);

export default {{resourceName}}Edit;