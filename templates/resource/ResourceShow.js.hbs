import React from 'react';

const {{resourceName}}Show = (props) => (
<Show {...props}>
  <SimpleShowLayout>
    {{#each components.showView}}
      {{{this}}}
    {{/each}}
  </SimpleShowLayout>
</Show>
);

export default {{resourceName}}Show;