import React from 'react';

const {{resourceName}}Edit = (props) => (
  <Edit {...props}>
    <SimpleForm>
    {{#each components.editView}}
      {{{this}}}
    {{/each}}
    </SimpleForm>
  </Edit>
);

export default {{resourceName}}Edit;