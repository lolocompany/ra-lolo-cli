import { ShowProps } from 'react-admin'
import { ResourceToolbar } from '@components/ResourceToolbar';

const {{resourceName}}Show: React.FC<ShowProps>  = (props) => (
<Show actions={<ResourceToolbar backButton editButton />} {...props}>
  <SimpleShowLayout>
    {{#each components.showView}}
      {{{this}}}
    {{/each}}
  </SimpleShowLayout>
</Show>
);

export default {{resourceName}}Show;